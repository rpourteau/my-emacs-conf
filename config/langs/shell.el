;;**********************************************************************
;; shell.el -- Configuration for Shell/bash
;;**********************************************************************
(use-package sh-script
  :straight nil  ;; built-in, no need to install
  :custom
  (sh-basic-offset 3)
  (sh-indentation 3))
